# Force LF line endings for all files that run inside Linux containers.
# Windows checkouts with core.autocrlf=true convert LF→CRLF, which breaks
# shell script shebangs and can cause subtle issues in Docker builds.

# Shell scripts — must have LF for shebangs to work
*.sh text eol=lf

# Python — prevent stray \r in container-executed scripts
*.py text eol=lf

# Dockerfiles and compose files
Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# nginx config templates
*.template text eol=lf
*.conf text eol=lf

# JSON data files
*.json text eol=lf

# Web assets — safe to keep LF everywhere
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf

# Git and Docker ignore files
.gitignore text eol=lf
.gitattributes text eol=lf
.dockerignore text eol=lf

# Env files
.env* text eol=lf

# Markdown
*.md text eol=lf
