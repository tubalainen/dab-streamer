{
  "name": "dab-streamer-api",
  "version": "1.0.0",
  "description": "API backend for dab-streamer — manages RTL-SDR devices, channel scanning, and audio stream proxying",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
